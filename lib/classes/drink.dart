// Datenklasse für Getränke (Bier, Wein, ...)
class Drink {
  String name;
  double volume; // volume percentage
  int quantity; // in milliliters

  Drink({
    this.name,
    this.volume,
    this.quantity,
  });
}
